//var Autol = new Mongo.Collection('_al');
/*if (Meteor.isServer) {
    Meteor.publish(key, function () {
        return masterConnection[key].find()
    })
}*/
// var lists = masterConnection.list.find().fetch()
// var aListCollections = []
// _.each(lists, function (value, key) {
//     aListCollections.push(value.content.list.sources.main.collection)
// })
// aListCollections = _.uniq(aListCollections)
// dbg(aListCollections)
